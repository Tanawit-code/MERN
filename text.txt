Backend
รับ request, ตรวจสิทธิ์, คุยกับ MongoDB, แล้วส่ง JSON กลับ

Frontend
รับ JSON จาก backend แล้วเอามาแสดงเป็นหน้า:
feed
groups
chat
friends
profile


git add .
git commit -m "update project"
git push --set-upstream origin main